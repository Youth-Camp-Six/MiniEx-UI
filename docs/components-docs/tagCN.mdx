import { Tag } from '../../src';
import { Switch } from '../../src';

# 标签

## 展示组件

### 单选

{' '}

<Tag type='primary' size={12} closable={true} close={() => alert('关闭了标签')}>
  可关闭的标签
</Tag>
### 开关
<Switch change={() => alert('关闭了开关')}></Switch>
## 展示代码

```js
const a = 0;
```

## API
