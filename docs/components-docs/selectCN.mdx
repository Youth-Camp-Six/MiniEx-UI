import { RadioSelect, MultipleSelect } from './demos/select/selectAppCN.tsx';
import ShowSouceCode from './components/show-source-code';
import PropsShow from './components/props-show';

# 选择器

### 单选

<ShowSouceCode

element={<RadioSelect />}
code={`
const header='选择标题'
const defaultValue1 = '';
const defaultValue2 = [];
const options = [
  {
    label: '广东',
    value: '1'
  },
  {
    label: '深圳',
    value: '2',
  },
  {
    label: '杭州',
    value: '3',
  },
  {
    label: '上海',
    value: '4',
  },
  {
    label: '北京',
    value: '5',
  },
];
const handleSelect = (e:Event) => {
  console.log(e);
}
return <Select header={header} modelValue={defaultValue} options={options}></Select>;` } />

### 多选

<ShowSouceCode

element={<MultipleSelect />}
code={
`const header='选择标题'
const defaultValue1 = '';
const defaultValue2 = [];
const options = [
  {
    label: '广东',
    value: '1'
  },
  {
    label: '深圳',
    value: '2',
  },
  {
    label: '杭州',
    value: '3',
  },
  {
    label: '上海',
    value: '4',
  },
  {
    label: '北京',
    value: '5',
  },
];

const handleSelect = (e:Event) => {
  console.log(e);
}

return <Select
        modelValue={defaultValue2}
        options={options}
        header={header}
        multiple={true}
        handleChange={(e) => handleSelect(e)}
      />;
` } />

## props

<PropsShow
  propsInfo={[
    {
      name: 'modelValue',
      description: '显示的值',
      type: 'string | array',
      initValue: "''",
    },
    {
      name: 'handleChange',
      description: '选择时回掉函数',
      type: 'function',
      initValue: '/',
    },
    {
      name: 'options',
      description: '选项',
      type: 'options',
      initValue: '[]',
    },
    {
      name: 'trigger',
      description: '触发方式',
      type: 'click | hover',
      initValue: 'click',
    },
    {
      name: 'multiple',
      description: '多选模式',
      type: 'boolean',
      initValue: 'false',
    },
  ]}
/>

## options

<PropsShow
  propsInfo={[
    {
      name: 'label',
      description: '选项组的标签',
      type: 'string',
      initValue: "''",
    },
    {
      name: 'value',
      description: '值',
      type: 'string',
      initValue: "''",
    },
  ]}
/>
